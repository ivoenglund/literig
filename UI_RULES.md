# Literig UI rules

- All confirmations, warnings, and error messages must appear in a centered modal.
- Never use the browser's native `alert()` or `confirm()` dialogs for user actions.
- Do not show important messages as notices pinned to the top or bottom of the screen.
- Keep destructive actions explicit and require confirmation before the action runs.
