# Life on Plants

Life on Plants is a plant-based food journal and recipe planner.

The application, database migrations, import tools, and deployment files all live
in this repository root. Run `npm start` to start the service locally.
